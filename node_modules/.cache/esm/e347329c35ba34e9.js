let express,Mongoose,cors,readdirSync;_35b‍.w("express",[["default",["express"],function(v){express=v}]]);_35b‍.w("mongoose",[["default",["Mongoose"],function(v){Mongoose=v}]]);_35b‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_35b‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);


 

const morgan = require("morgan");
require("dotenv").config();
const app = express();

Mongoose.connect(process.env.DATABASE, {
  useNewUrlParser: true,
  //   useCreateIndex: true,
  //     useFindAndModify: false,
  useUnifiedTopology: true,
})
  .then(() => console.log("DB connected"))
  .catch((err) => _35b‍.g.console.log("DB CONNECTION ERROR =>", err));

app.use(express.json({ limit: "5mb" }));
app.use(express.urlencoded({ extended: true }));
app.use(
  cors({
    origin: ["http://localhost:3000"],
  })
);
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));
const port = process.env.PORT || 8000;
app.listen(port, () => console.log(`server running on port ${port}`));
